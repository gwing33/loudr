box: wercker/nodejs@0.0.10
services:
  - wercker/mongodb@0.0.1
  - wercker/redis
build:
  steps:
    - npm-install@1.0.5:
        strict-ssl: false
    - jshint:
        use_strict: true
        trailing_whitespace: false
    -  npm-test
# deploy:
#   steps:
#     - compass-compile@1.0:
#         version: 4.1.2
#         output: compressed